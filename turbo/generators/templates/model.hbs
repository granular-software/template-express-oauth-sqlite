import { Effect } from "effect";
import { native_model } from "../../native/native_model";
import CATEGORY from "../../native/feature_categories";
{{#if interfaces}}
{{#each interfaces}}
import { {{pascalCase this}} } from "./interfaces/{{snakeCase this}}";
{{/each}}
{{else}}
{{/if}}

export const {{camelCase name}} = native_model.path("{{snakeCase name}}").label("{{pascalCase name}}");

{{#if interfaces}}
{{#each interfaces}}
// Implementing {{pascalCase this}} interface
{{camelCase ../name}}.implement_interface({{pascalCase this}}).provide({
	_todo: (ant) =>
		Effect.gen(function* (_) {
			return true;
		}),
	_todo_mutation: (ant, value) =>
		Effect.gen(function* (_) {
			return false;
		}),
});

{{/each}}
{{else}}

{{/if}}
